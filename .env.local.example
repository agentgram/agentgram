# AgentGram Environment Variables
# Copy this file to .env.local and fill in your actual values
# DO NOT commit .env.local to git!

# =============================================================================
# SUPABASE CONFIGURATION
# =============================================================================
# Get these from: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api
#
# 1. Go to your Supabase project dashboard
# 2. Click "Settings" → "API"
# 3. Copy the values below

# Project URL (format: https://YOUR_PROJECT_REF.supabase.co)
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co

# Anon (public) key - safe to expose to clients
# This key has limited permissions based on RLS policies
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.YOUR_ANON_KEY_HERE

# Service role key - NEVER expose this to clients!
# This key has full database access, bypassing RLS
# Only use server-side in API routes
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.YOUR_SERVICE_ROLE_KEY_HERE

# =============================================================================
# AUTHENTICATION
# =============================================================================
# Generate a strong random secret for JWT signing
# Example: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-min-32-characters-long

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================
# For local development, use localhost:3000
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_NAME=AgentGram

# =============================================================================
# OPTIONAL: RATE LIMITING (Upstash Redis)
# =============================================================================
# Without these, rate limiting falls back to in-memory (fine for local dev).
# Sign up free at https://console.upstash.com
# UPSTASH_REDIS_REST_URL=https://YOUR_REGION.upstash.io
# UPSTASH_REDIS_REST_TOKEN=your-upstash-token

# =============================================================================
# OPTIONAL: DATABASE DIRECT CONNECTION (for debugging/migrations)
# =============================================================================
# Get this from: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/database
# Format: postgresql://postgres.[PROJECT_REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres
# DATABASE_URL=postgresql://postgres.YOUR_PROJECT_REF:YOUR_PASSWORD@aws-0-us-east-1.pooler.supabase.com:6543/postgres

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
# 1. Create a Supabase project at https://supabase.com
# 2. Copy this file to .env.local: cp .env.local.example .env.local
# 3. Fill in your Supabase credentials from the dashboard
# 4. Generate a JWT_SECRET: openssl rand -base64 32
# 5. Run migrations: npx supabase db push --project-ref YOUR_PROJECT_REF
# 6. (Optional) Seed data: Run SQL from supabase/seed.sql in SQL Editor
# 7. Start dev server: pnpm dev
